{
  "name": "backend",
  "version": "1.0.0",
  "description": "family labum",
  "private": true,
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon -e ts,js,json --watch ./src --watch server.ts --exec 'npm run build; node ./dist/server.js'",
    "clean": "rm -rf ./dist/*",
    "build": "npm run clean; tsc -p tsconfig.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^13.9.5",
    "koa": "^2.11.0",
    "postgres": "^1.0.2",
    "typescript": "^3.8.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
